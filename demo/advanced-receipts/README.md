# Advanced evidence receipts

Place one accepted or candidate JSON receipt per demonstration group here:

- `AD1.json` through `AD9.json`

Use `../advanced-receipt.schema.json` and the acceptance requirements in `../ADVANCED-RECEIPT-PLAN.md`.

No receipt exists yet. All groups remain `hold` in `../advanced-receipt-manifest.json`.

Never store customer data, real account credentials, wallet addresses tied to a person, seed phrases, private keys, passphrases, xprvs, PINs, passwords, authentication codes, or unredacted confidential agreements in this directory.
